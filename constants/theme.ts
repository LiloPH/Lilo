export type Theme = {
  background: string;
  textColor: string;
};

export const lightTheme: Theme = {
  background: "white",
  textColor: "black",
};

export const darkTheme: Theme = {
  background: "#1e293b",
  textColor: "white",
};
